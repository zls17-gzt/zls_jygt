# 家园沟通站 · 方式B（PWA）搭建与测试说明

## 一、已生成的文件清单

```
jiayuan-workbench/
├── index.html        ← B0 骨架 + B1-3 SW 注册 + B2 数据层 + B3 周报 + 切换 Tab
├── manifest.json     ← B1-1 PWA 名片
├── sw.js             ← B1-2 离线缓存管家
├── icon-192.png      ← B1-4 安装图标（192×192）
├── icon-512.png      ← B1-4 安装图标（512×512）
├── preview.html      ← 离线双击预览入口（无服务器、无 SW）
├── start-server.bat  ← 双击启动本地预览服务器
└── gen_icons.py      ← 生成图标的纯 Python 脚本（可删除）
```

**关于图标**：当前图标是我用 `gen_icons.py` 生成的临时占位图（金色底 + 小房子）。手册 B1-4 推荐后续到 [pwabuilder.com/imageGenerator](https://www.pwabuilder.com/imageGenerator) 上传真实班级/学校 Logo 后替换 `icon-192.png` 和 `icon-512.png`，手机桌面上会更正式。

---

## 二、B0 项目骨架：index.html

### 部署方式
1. 在任意位置新建文件夹，命名为 `jiayuan-workbench/`。
2. 把生成的 `index.html` 保存进去。
3. 用浏览器（Chrome/Edge/Safari）直接双击打开 `index.html`。

### 本地测试方法
- **方式一（推荐，PWA 功能完整）**：在文件夹里双击 `start-server.bat`，等黑窗口出现 `Serving HTTP on ::1 port 8081` 后，用浏览器访问 `http://localhost:8081/`。
- **方式二（快速查看，无 SW，推荐初次预览）**：直接用浏览器双击打开 `preview.html`（或 `index.html`）。这是离线预览入口，不需要服务器、不需要联网，节假日卡片、设置等界面都能直接看。注：双击打开时 Service Worker 不会生效，但不影响功能体验。
- 页面打开后，底部应出现四个 Tab：📝 记录 / 📊 周报 / 👶 孩子 / ⚙️ 设置。
- 分别点击 Tab，页面内容应切换。
- 在「记录」页，下拉框、文本框、保存按钮都应能点击；在「孩子」页，输入框、添加按钮也应能点。

### 预期结果
- 三个 Tab 切换正常。
- 此时点击「保存记录」不会报错，但刷新页面后数据会消失（因为 B2 数据层还没接入）。
- **对应手册验证**："双击 index.html 用浏览器打开——能看到三个 Tab 切换、表单能点。"

---

## 三、B1 PWA 三件套：manifest.json + sw.js + 图标 + SW 注册

### 部署方式
1. 把 `manifest.json`、`sw.js`、`icon-192.png`、`icon-512.png` 全部放到 `jiayuan-workbench/` 根目录，与 `index.html` 平级。
2. `index.html` 的 `<head>` 里已经写好了：
   ```html
   <link rel="manifest" href="manifest.json">
   <link rel="apple-touch-icon" href="icon-192.png">
   ```
3. `index.html` 的 `<script>` 最开头已经注册了 Service Worker：
   ```js
   if ('serviceWorker' in navigator) {
     navigator.serviceWorker.register('./sw.js')
   }
   ```
4. **重要**：Service Worker 在本地 `file://` 打开时不会生效，必须部署到 `https://` 地址后才生效（见 B4）。

### 本地测试方法（部署前能测的部分）
- 打开浏览器开发者工具（F12）→ Console。
- 如果通过本地 `file://` 打开，应看到没有 Service Worker 报错，因为代码只在 `navigator.serviceWorker` 存在时才注册。
- 在本地测试时，manifest 也不会生效，这属于正常情况。

### 部署后的测试方法
- 把项目部署到 GitHub Pages 后，用 Chrome/Edge 打开网址。
- F12 → Application → Service Workers，应能看到 `sw.js` 已激活（Status: activated）。
- F12 → Application → Manifest，应能看到 Name：家园沟通站、Display：standalone、Icons 正常显示。

### 预期结果
- 部署后，Chrome 地址栏右侧出现「安装」按钮；iOS Safari 分享菜单出现「添加到主屏幕」。
- **对应手册验证**："部署后电脑浏览器打开这个网址，能正常使用。"

---

## 四、B2 数据层：localStorage 存取

### 部署方式
- 数据层代码已经按手册要求插入到 `index.html` 的 `<script>` 中，位于 `switchTab` 函数之前、SW 注册之后。
- 无需单独部署文件，只要 `index.html` 正确保存即可。

### 本地测试方法
1. 刷新页面。
2. 点底部 Tab 切换到「👶 孩子」，输入「糖糖」，点「添加」。
3. 切换回「📝 记录」，幼儿多选列表里应出现「糖糖」，勾选「糖糖」，区域任选（如「建构区」），文本框写"今天我在建构区搭了一个大城堡"，点「保存记录」。
4. 关闭浏览器标签，重新打开 `index.html`。

### 预期结果
- 刷新后，「孩子」页里仍有「糖糖」。
- 「记录」页里仍有刚才保存的「建构区」记录。
- **对应手册验证**："刷新页面 → 孩子 Tab 添加'糖糖' → 记录 Tab 选糖糖写一条记录保存 → 刷新页面数据还在 = 数据层 OK。"

---

## 五、B3 周报生成：模板拼装

### 部署方式
- `genReport()` 函数已经按手册要求插入到 `index.html` 的 `<script>` 中，位于数据层之后、`switchTab` 函数之前。
- 无需单独部署文件。

### 本地测试方法
1. 先确保已有孩子（如「糖糖」）。
2. 在「📝 记录」页，为同一个孩子添加 **多条不同区域** 的记录，例如：
   - 建构区：今天搭城堡。
   - 美工区：画了一幅彩虹。
   - 建构区：又搭了条马路。
3. 切换到「📊 周报」，选择孩子「糖糖」，点「生成周报文字」。
4. 点「复制周报」。

### 预期结果
- 文本框里出现以 `🌟 糖糖 本周成长周报（...）` 开头的整段文字。
- 文字按区域做了分组统计，并抽取了代表性语录。
- 点击「复制周报」后，在微信聊天框长按粘贴能发出完整文本。
- **对应手册验证**："先存几条不同区域的记录 → 周报 Tab 选孩子 → 点生成 → 文本框出现规整周报 → 点复制 → 去微信粘贴发送成功。"

---

## 六、B4 部署到 GitHub Pages（让 PWA 真正可用）

方式B必须跑在 `https://` 下，SW 和 manifest 才能生效。手册推荐 GitHub Pages，**免费、永久、完全自主**（不依赖任何第三方托管），只要你的 GitHub 账号和仓库在，网址就一直在。

> 本项目代码已在本机 `jiayuan-workbench/` 目录完成 `git init` 并提交了初始版本。下面给两种部署方式，**任选其一**。

### 方式一：网页拖拽上传（推荐，零安装）

**B4-1 注册 GitHub 账号**
1. 打开 https://github.com → Sign up → 用邮箱注册 → 完成邮箱验证。

**B4-2 创建仓库**
1. 登录后右上角点 `+` → New repository。
2. Repository name 填：`jiayuan-workbench`。
3. 选 **Public**（Private 无法用免费 Pages）。
4. 勾选 **Add a README file** → Create repository。

**B4-3 上传文件**
1. 仓库页点 **Add file** → Upload files。
2. 打开你电脑里的文件夹 `C:\Users\15770\WorkBuddy\2026-08-18-20-33-05\jiayuan-workbench\`，把下面 **6 个文件** 拖进网页：
   - `index.html`
   - `manifest.json`
   - `sw.js`
   - `icon-192.png`
   - `icon-512.png`
   - `preview.html`
3. README.md 保留不动。
4. 点 **Commit changes**。
5. **关键检查**：确认文件名全是小写（`index.html` 不是 `Index.html`）。

> 注：本目录里还有一个隐藏文件 `.nojekyll`，**本项目用不到它**（没有下划线开头的文件，Jekyll 不会误处理），网页上传时忽略即可；若以后用命令行 `git push` 方式部署，它会一起传上去，无害。

**B4-4 开启 GitHub Pages**
1. 仓库页 → Settings → 左侧 **Pages**。
2. Source 选 **Deploy from a branch**，Branch 选 `main`，文件夹选 `/(root)` → Save。
3. 等 1~3 分钟，页面顶部出现绿色提示，显示网址：
   ```
   https://你的用户名.github.io/jiayuan-workbench/
   ```

### 方式二：命令行一键发布（进阶，我可代跑）

如果你愿意装一个叫 `gh` 的小工具并登录一次 GitHub，之后发布我可以用脚本一键完成：
1. 安装 GitHub CLI：https://cli.github.com/ 下载 Windows 版安装。
2. 在终端运行 `gh auth login`，按提示用浏览器登录（会给你一串 code 复制过去）。
3. 告诉我你已经登录好，我帮你执行：创建仓库 → 推送代码 → 开启 Pages，全程不用你再点。
> 注意：`gh auth login` 必须你本人操作（要登录你的 GitHub 账号），我无法替你登录。

### 验证方法
- 打开 `https://你的用户名.github.io/jiayuan-workbench/`，能正常添加孩子、保存记录、生成周报。
- 手机用 Safari/Chrome 打开同一网址，能正常操作。
- 手机「添加到主屏幕」后像 App 一样全屏打开（见 B5）。

### 预期结果
- 网址永久可访问，功能正常，离线也能打开（SW 在 https 下生效）。
- **对应手册验证**："电脑浏览器打开这个网址，能正常使用 = 部署成功。"

---

## 七、B5 手机添加到桌面

### iPhone（iOS Safari）
1. 用 **Safari**（微信内置浏览器不行）打开部署后的网址。
2. 点底部「分享」按钮（方框带向上箭头）。
3. 往下滑，点「添加到主屏幕」。
4. 名称可改短（如「家园站」）→ 点右上角「添加」。

### Android（Chrome / 小米/华为浏览器）
1. 用 Chrome（或系统自带浏览器）打开部署后的网址。
2. 点右上角 ⋮ 菜单。
3. 点「添加到主屏幕」/「安装应用」。
4. 确认添加。

### 验证方法
| 验证项 | 操作 | 预期 |
|---|---|---|
| 桌面图标 | 回到手机桌面 | 出现 🏠 图标 |
| 全屏打开 | 点图标 | 无浏览器地址栏，全屏界面 |
| 数据留存 | 加一条记录 → 关掉 → 再开 | 记录还在 |
| 离线可用 | 开飞行模式 → 点图标 | 仍能打开看到数据 |

---

## 八、B6 排错表

| 现象 | 原因 | 处理 |
|---|---|---|
| 添加到主屏幕后打开仍有地址栏 | `manifest.json` 的 `display` 不是 `standalone`；或用了非 Safari/Chrome | 检查 `manifest.json` 的 `"display": "standalone"`；iOS 用 Safari 重新添加 |
| 飞行模式打开白屏 | `sw.js` 没生效：网址不是 https / 没注册成功 | 确认网址是 `https://` 开头；电脑 Chrome F12 → Application → Service Workers 查看状态 |
| 改了代码手机上没变化 | 缓存了旧版 | 改 `sw.js` 里的 `CACHE_NAME` 版本号（如 `jiayuan-v2` → `jiayuan-v3`），重新上传；手机端关掉重开 |
| 数据全没了 | 清了浏览器数据/换了浏览器/用了无痕模式 | 无解，只能重新录入——建议每周导出备份 |
| Pages 显示 404 | 文件名大小写不一致 | 上传的文件名全部小写核对一遍 |
| GitHub 打不开 | 网络原因 | 换时间重试；或改用 Vercel/Gitee Pages |

---

## 九、补充功能：导出备份 + AI 润色（已实现）

这两项已按手册描述的方式补进 `index.html`，新增了一个「⚙️ 设置」Tab（第 4 个 Tab）。

### 9.1 数据导出备份

- **手册依据**：B2 末尾「建议每周用『设置 → 导出』导出一次 JSON 备份（导出功能按上面 load() 返回值下载成文件即可，属通用技能，自行补10行）」。
- **实现方式**：`exportBackup()` 把 `load()` 的返回值（`{children, records}`）用 `Blob` 下载成 `jiayuan-backup-日期.json`，**没有用任何非手册 API**。
- **部署**：代码已并入 `index.html`，无需单独文件。

**测试方法**：
1. 先添加孩子并保存几条记录。
2. 切到「⚙️ 设置」Tab，点「💾 数据备份 → 导出备份（JSON）」。
3. 浏览器应下载一个 `jiayuan-backup-YYYY-MM-DD.json` 文件。

**预期结果**：打开该 JSON，里面包含你录入的孩子和记录数组，可用于日后恢复或迁移。

### 9.2 AI 润色周报

- **手册依据**：B3 末尾「把 report 作为素材调用 DeepSeek API 润色成更生动的文字……在页面里加一个『设置』输入框存密钥」。
- **实现方式**：
  - 「⚙️ 设置」页有 API Key 输入框，`saveKey()` 把密钥存进 `localStorage`（键名 `jiayuan-deepseek-key`），**不写死在代码里**，符合手册两点要求 ①。
  - 周报页新增「✨ AI 润色」按钮，调用 `https://api.deepseek.com/v1/chat/completions`（`deepseek-chat`，temperature 0.6）。
  - 调用失败（含 CORS 限制）时 `catch` 提示并**保留原有模板文字**，符合手册要求 ②。
- **你需要做的**：自己到 [platform.deepseek.com](https://platform.deepseek.com) 注册充值，拿到 Key，在设置页粘贴保存。

**测试方法**：
1. 在「⚙️ 设置」填好 DeepSeek API Key 并保存。
2. 到「📊 周报」页，选孩子生成周报文字。
3. 点「✨ AI 润色」→ 确认。
4. 周报输出区里的文字被替换为更生动的润色版，原有图片仍保留，提示「AI 润色完成 ✨」。

**预期结果**：
- 成功：周报输出区变成 AI 润色后的温暖文字，原有图片继续显示，可直接「复制周报文字」发家长。
- 失败（无 Key / 网络 / CORS / Key 无效）：弹窗提示失败原因，并保留原来的模板周报，基本功能不受影响。

> ⚠️ **CORS 提示**：浏览器直连 DeepSeek 可能因跨域被拦（手册已预警）。若你实测一直失败，这是浏览器安全策略限制，不是代码错误；想要稳定用 AI 正是手册所说"选方式A"的理由。届时可在方式A的云函数里中转调用（手册 A2 的 `http-request` 已为此设计）。

---

## 十、新增功能：多名幼儿 + 图片插入

这两项是你在本地验证阶段提出的增强需求，**不属于手册原文**，已按最小改动原则接入 `index.html`。

### 10.1 多名幼儿

- **改动**：「📝 记录」页的「幼儿姓名」从单选下拉改成了 checkbox 列表，可一次勾选多名幼儿。
- **数据结构**：记录对象从 `childId` / `childName` 改成了 `childIds: []` / `childNames: []` 数组。
- **向后兼容**：打开页面时，`load()` 会自动把旧记录（单 childId）迁移成新格式，已有数据不会丢失。
- **周报处理**：周报仍按单个孩子生成；只要记录里 `childIds` 包含该孩子，这条记录就会进入周报统计。

**测试方法**：
1. 在「👶 孩子」页添加 2~3 个孩子（如「糖糖」「果果」）。
2. 在「📝 记录」页同时勾选「糖糖」和「果果」，选区域、填文字，保存。
3. 查看今日记录列表，卡片上应显示「糖糖、果果」。
4. 分别到「📊 周报」页选「糖糖」和「果果」生成周报，两人都应出现这条记录。

### 10.2 插入图片

- **改动**：「📝 记录」页在文字框下方增加了图片上传区，点击「+」选择图片。
- **限制**：最多 3 张；自动压缩到宽度 ≤ 1024px、JPEG 质量 0.8，减少 base64 体积。
- **存储**：图片以 base64 字符串存入记录的 `images` 数组，随 `localStorage` 一起保存。
- **展示**：今日记录列表里，每条记录下方会显示插入的图片。

**测试方法**：
1. 在「📝 记录」页选择幼儿、区域、文字。
2. 点「+」选 1~3 张照片（可多选）。
3. 照片会以 72×72 缩略图显示在「+」按钮左侧。
4. 点「保存记录」，记录卡片下方出现大图。
5. 刷新页面，图片仍在。

> ⚠️ **容量提示**：`localStorage` 一般只有 5~10MB。照片虽然压缩过，但大量高清图仍会撑满。建议只把照片作为点缀（1~3张/记录），不要当相册用。如果后续照片很多，需要升级到方式A用云存储。

### 10.3 编辑与删除记录

- **改动**：今日记录列表的每条卡片下方新增了「✏️ 编辑」按钮。
- **编辑流程**：点击编辑 → 该记录的幼儿、区域、文字、图片回填到上方表单 → 修改后点「更新记录」→ 原记录被更新，不会新增一条。
- **图片编辑**：编辑模式下，已插入图片右上角会出现「×」，点击可删除单张；也可以继续点「+」添加新图（仍最多 3 张）。
- **删除记录**：编辑模式下会出现「🗑 删除记录」按钮，确认后删除整条记录；点「取消编辑」可退出编辑模式并清空表单。

**测试方法**：
1. 保存一条记录。
2. 在记录卡片上点「✏️ 编辑」——上方表单应被回填。
3. 修改文字或换几张图，点「更新记录」——记录列表里同一条记录被更新，没有新增。
4. 再点「编辑」→「🗑 删除记录」→ 确认——该记录从列表消失。

### 10.4 周报里展示图片

- **改动**：「📊 周报」页输出区从 `<textarea>` 改为可渲染 HTML 的 `<div class="report-output">`，生成周报时自动收集该幼儿相关记录里的图片并一起展示。
- **图片来源**：周报所选幼儿在最近 7 天记录里 `images` 数组中的 base64 图片，去重后最多展示 6 张。
- **导出逻辑**：「🖼 导出周报图片」按钮用纯 canvas 把当前周报的**文字 + 图片记录**合成一张 PNG 图片并下载（高清 2 倍、离线可用，不依赖任何外部库）。图片按每行 2 张排布，最多展示 6 张。
- **AI 润色**：润色只修改文字，`currentReportImages` 保存当前周报图片，润色后导出图片的文字已是润色版、图片继续保留。

**测试方法**：
1. 给某个孩子保存 1~3 条带图片的记录。
2. 到「📊 周报」页选择该孩子，点「生成周报文字」。
3. 文字下方应出现相关记录的图片（一行约 2 张）。
4. 点「🖼 导出周报图片」→ 浏览器下载一张 `周报-日期.png`。
5. 打开该 PNG，应能看到周报文字 + 记录里的照片都在同一张图里。
6. 若已配置 DeepSeek Key，先点「✨ AI 润色」再导出，图片里的文字会是润色版、照片不变。

### 10.5 孩子完整信息字段与 CSV 导入

- **改动**：「👶 孩子」页从只填姓名扩展为：姓名、性别、生日、身份证号、家长姓名、家长手机号、备注。手动添加和 CSV 批量导入都支持这些字段。
- **家长手机号支持多个**：一个孩子的家长手机号存为数组，方便联系爸爸、妈妈等多位家长。填写时用**空格或逗号分隔**（如 `13800138000 13900139000`）；CSV 模板同理。每个号码都校验为 11 位数字。详情里每个号码都是 `tel:` 拨号链接，点击可直接拨打。
- **CSV 模板**：页面内提供「下载 CSV 模板」按钮，生成 `.csv` 文件（带 UTF-8 BOM，Excel/WPS 可直接打开）。模板含表头：姓名、性别、生日、身份证号、家长姓名、家长手机号、备注，并附一条多号码示例数据。
- **导入逻辑**：选择 CSV 后按表头解析，自动跳过空行；校验姓名必填、家长手机号每个 11 位；导入成功显示数量，失败行显示原因。
- **列表展示**：孩子卡片默认显示姓名和所有家长手机号；点击卡片展开完整信息（性别、生日、身份证、家长姓名、手机号拨号链接、备注）。
- **向后兼容**：旧数据只有 `name` 或单个 `parentPhone` 字段的，打开会自动迁移成新格式，不丢失。
- **数据安全**：身份证、手机号等敏感信息仅存在浏览器 `localStorage`，不上传任何服务器。

**测试方法**：
1. 手动添加：在「👶 孩子」页填姓名，家长手机号填 `13800138000 13900139000` → 添加 → 列表显示两个号码 → 点击卡片展开可见两个 `tel:` 拨号链接。
2. 下载模板：点「下载 CSV 模板」→ 用 Excel/WPS 打开 → 按示例填多个孩子（含多号码）→ **另存为 CSV UTF-8**。
3. 导入：点「导入 CSV 文件」→ 选择刚保存的 CSV → 提示导入成功，列表自动刷新。
4. 验证：点击导入的孩子卡片，应能看到姓名、身份证、多个手机号等信息。
5. 异常测试：某行不写姓名、或某号码写 10 位 → 导入时该行失败提示，其他正确行仍成功。

### 10.6 班级切换与管理

- **改动**：数据模型从单一 `{children, records}` 改为 `{classes: [...], currentClassId, holidays: [...]}`，支持多班级隔离数据。
- **班级切换**：记录页、周报页、孩子页顶部都出现「当前班级」下拉框，切换后该页面及所有相关数据（孩子、记录、周报）自动跟随切换。
- **班级管理**：「⚙️ 设置」页增加班级管理卡片，可添加新班级、重命名班级、删除班级（至少保留一个）。
- **向后兼容**：旧数据（没有 `classes` 数组）打开会自动迁移成「默认班级」，原有孩子与记录不丢失。
- **数据隔离**：每个班级独立拥有 `children` 和 `records`，切换班级互不干扰。

**测试方法**：
1. 打开页面，顶部应显示「当前班级：默认班级」。
2. 在「👶 孩子」页添加孩子 A。
3. 到「⚙️ 设置」→ 班级管理 → 输入「小二班」→ 添加。
4. 返回「👶 孩子」页，顶部切换为「小二班」→ 列表为空（新班级）。
5. 在小二班添加孩子 B。
6. 切换回默认班级 → 应只看到孩子 A；切换到小二班 → 应只看到孩子 B。
7. 在「📝 记录」页同样验证：不同班级的记录互不干扰。
8. 在设置页重命名某个班级 → 顶部下拉框名称同步更新。
9. 删除非当前班级 → 确认后该班级消失；尝试删除最后一个班级 → 提示至少保留一个。

### 10.7 节假日倒计时

- **改动**：「⚙️ 设置」页新增节假日倒计时卡片。
- **内置节假日**：默认内置 2026 年常见国家节假日（元旦、春节、清明、劳动、端午、中秋、国庆），按距离今天的天数排序，未过的在前。
- **自定义节假日**：可添加自己的节假日（如「春游」「六一汇演」），自定义节假日显示在「我的自定义节假日」列表，可单独删除。
- **日历卡片网格**：节假日以卡片网格展示，每张卡片显示「几月 / 几日 / 星期几 / 名称 / 倒计时」，视觉上类似教师工作日历。
- **倒计时规则**：今天 = 0 天时高亮显示「🎉 今天」；未来显示「还有 N 天」；过去显示「已过 N 天」。
- **过去的日子加下划线**：已过的节假日名称会带下划线，卡片整体变浅灰，便于一眼区分。
- **自定义节假日**：可添加自己的节假日（如「春游」「六一汇演」），自定义节假日显示在「我的自定义节假日」列表，可单独删除。

**测试方法**：
1. 打开「⚙️ 设置」→ 节假日倒计时，应看到内置节假日以日历卡片网格展示，未过的排在前面、已过的排在后面并带下划线。
2. 输入名称「春游」、日期选明天 → 添加 → 卡片网格顶部应出现「春游」卡片，显示「还有 1 天」。
3. 在「我的自定义节假日」里点删除 → 该节假日从卡片网格和自定义列表同时消失。
4. 刷新页面，已添加的自定义节假日仍在。

---

## 十一、当前完整文件清单

```
jiayuan-workbench/
├── index.html        ← B0骨架 + B1-3 SW注册 + B2 数据层 + B3 周报 + 设置/导出/AI润色/班级/节假日 + 多名幼儿/图片/编辑删除 + 孩子信息CSV导入 + Tab
├── manifest.json     ← B1-1
├── sw.js             ← B1-2（当前缓存版本 jiayuan-v10）
├── icon-192.png      ← B1-4 占位图标
├── icon-512.png      ← B1-4 占位图标
├── gen_icons.py      ← 生成图标的脚本（可删）
└── 搭建与测试说明.md ← 本文件
```

